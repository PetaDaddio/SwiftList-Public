# SwiftList Lifeguard - Error Analyzer Lambda Requirements

# Claude API
anthropic==0.42.0

# HTTP requests
requests==2.31.0

# AWS SDK (boto3 is included in Lambda runtime, but specifying for local testing)
boto3==1.34.0
botocore==1.34.0

# JSON/data handling
python-dateutil==2.8.2
